/**
 * @module adviz/geometry
 *
 * GPU mesh primitives and custom geometry builders.
 *
 * @example
 * import { createTriangle, createCircle, createFullscreenQuad } from 'adviz/geometry'
 */

export { createFullscreenQuad, createTriangle, createCircle } from './primitives.js'
export type { Mesh } from './primitives.js'
